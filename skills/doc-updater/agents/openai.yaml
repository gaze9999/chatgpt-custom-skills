interface:
  display_name: "Doc Updater"
  short_description: "Align existing docs from verified implementation changes"
  icon_small: "./assets/icon-small.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $doc-updater to minimally update identified existing documentation from concrete implementation evidence. Access or report on Notion only when the request explicitly names Notion or a Notion target."

policy:
  allow_implicit_invocation: true
