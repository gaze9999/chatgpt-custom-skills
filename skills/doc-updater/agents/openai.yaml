interface:
  display_name: "Doc Updater"
  short_description: "Align existing docs from verified implementation changes"
  icon_small: "./assets/icon-small.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $doc-updater to minimally update identified existing documentation from concrete implementation evidence; access Notion or sync both targets only when the current request explicitly includes them."

policy:
  allow_implicit_invocation: true
