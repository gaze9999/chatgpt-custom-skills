interface:
  display_name: "AI Application Engineering"
  short_description: "Engineer LLM, agent, tool, and RAG applications"
  default_prompt: "Use $ai-application-engineering to inspect the current AI stack, preserve its provider and permission boundaries, implement the requested LLM, agent, tool-calling, RAG, embedding, or runtime change, and verify the affected behavior."

policy:
  allow_implicit_invocation: true
