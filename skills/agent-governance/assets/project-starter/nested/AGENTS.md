# <Directory> agent rules

<!-- Copy to the real directory. Keep only facts that differ from the root. -->

- Owns: <verified responsibility and paths>
- Runtime and tools: <verified versions and source of truth>
- Public boundary: <interfaces and other owners affected by a change>
- Local patterns: <relevant source or configuration pointers>
- Focused checks: <actual commands and scope>

Follow root `AGENTS.md` for shared boundaries. Changes outside this subtree require the relevant owner and task authorization. State which integration checks remain unrun.
