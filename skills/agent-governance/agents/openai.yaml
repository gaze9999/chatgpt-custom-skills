interface:
  display_name: "Agent Governance"
  short_description: "Simplify agent governance and clarify role boundaries"
  icon_small: "./assets/icon-small.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $agent-governance only for AGENTS.md, nested instruction, role, model-routing, or context-governance work. Keep root guidance portable and repository-wide, move directory-specific rules to the nearest nested layer, and load only the relevant detailed reference."

policy:
  allow_implicit_invocation: true
